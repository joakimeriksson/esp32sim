diff --git a/xtensa-lx7/src/jit/wasm_memory.rs b/xtensa-lx7/src/jit/wasm_memory.rs
index 8e95d67a..d488d381 100644
--- a/xtensa-lx7/src/jit/wasm_memory.rs
+++ b/xtensa-lx7/src/jit/wasm_memory.rs
@@ -216,7 +216,7 @@ pub(super) fn probe(g: &mut Gen, width: u32, store: bool) {
 pub struct SpecArea { log: [u8; 1 << 20], pad: [u8; 64], gen: [u32; 2], logp: u32, pad2: u32, r: [u32; 1 << 20], w: [u32; 1 << 20] }
 pub static mut SPEC: SpecArea = SpecArea { log: [0; 1 << 20], pad: [0; 64], gen: [0; 2], logp: 0, pad2: 0, r: [0; 1 << 20], w: [0; 1 << 20] };
 pub static SPEC_ROLE: std::sync::atomic::AtomicU8 = std::sync::atomic::AtomicU8::new(0);
-pub static SPEC_MODE: std::sync::atomic::AtomicU8 = std::sync::atomic::AtomicU8::new(match option_env!("SPEC_MODE") { Some("0") => 0, Some("1") => 1, Some("2") => 2, _ => 3 }); // bit0 marks/checks, bit1 undo log
+pub static SPEC_MODE: std::sync::atomic::AtomicU8 = std::sync::atomic::AtomicU8::new(match option_env!("SPEC_MODE") { Some(m) => m.as_bytes()[0] - b'0', None => 3 }); // bit0 marks/checks, bit1 undo log
 
 #[allow(static_mut_refs)]
 fn spec_tax(g: &mut Gen, width: u32, store: bool) {
