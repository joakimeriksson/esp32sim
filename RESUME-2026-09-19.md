# Paused review and WASM performance work

Paused at the user's request on September 19, 2026. This is an unfinished local
checkpoint, not a completed review or a passing release candidate. No changes from
this task have been pushed and no PR has been created.

## Resume first

1. Fix the known scheduler regression before further integration: in
   `esp-soc/src/machine.rs`, a virtual batch can execute WAITI after three steps,
   leave `resume_at = 3` and expose a timer one cycle away. The new timer clamp
   uses that already-advanced core's deadline, makes `quantum = 1` and underflows
   `quantum - resume_at`. Restrict this clamp to cores idle at the original round
   start (`on[i] && idle[i]`) or correctly translate the deadline. Add a regression
   comparing VQ enabled/disabled for WAITI mid-round. Independent reviewer found
   this after the earlier scheduler tests passed.
2. Recheck all open GitHub PR heads. Incorporate new changes without overwriting
   anyone's published history. The last refresh found new #86–#89 review fixes;
   their source changes were integrated equivalently but their ancestry is NOT yet
   merged. Normal-merge `origin/web/smooth-drawing` after checking the equivalence.
3. Run the final release workspace suite, clippy, production WASM manifest tests
   and default WASM differential suite on a frozen tree. Do not blindly update
   goldens: the corrected mixed-idle timer deadline changes scheduling legitimately,
   but console/audio differences require investigation. The earlier whole-workspace
   release run stopped on two RMT tests during concurrent edits; both subsequently
   passed a fresh release run. The complete final gate has NOT passed yet.
4. Rerun the F64 regression after its last test-only change. CUST0/1 really encode
   PIE floating loads/stores; their exact CP0/CP3 gate remains an open fact-check.
   Product gating was preserved. The final functional test enables both CPs.
5. Continue the pocket-tank experiments in the separate performance worktree.
   Finish matched repeated comparisons and then port retained changes to this tree.
6. Complete the three-review synthesis, final source review and communications
   review. Record performance outcomes in the canonical `docs/experiments.md`,
   retaining negatives and earlier results. Preserve compact durable test receipts
   under docs/evidence before creating any PR.

## Worktrees and commits

- Main review: `/Users/alice/src/a/esp32sim/work/review-perf-0919`, branch
  `codex/review-perf-0919`. The user's original main checkout was not edited.
- Performance: `/Users/alice/src/a/esp32sim/work/pocket-perf-0919`, branch
  `codex/pocket-perf-0919`, candidate commit `e1233676`.
- Frozen performance baseline: `/Users/alice/src/a/esp32sim/work/pocket-baseline-0919`,
  commit `cfa669c4461e6c5b06f9740b2554686830c8d8de`.
- Performance artifacts/receipts: `/Users/alice/src/a/esp32sim/work/pocket-perf-runs`.
- Initial combined baseline included #85, #86, #87, #88, #89, #91, #92 and #93.
  The top was #91 `b0271570`; #92 was `abbbf688`; #93 was `87da306f`.
- Latest fetched #86–#89 heads: `3aa74620`, `12109bea`, `322c1038`, `5a1bba82`.
  Their VQ stop/deadline/disabled-scan fixes and worker ACK/pacing fixes are in the
  current source. Upstream tests were included with #91 timing tests preserved.

Recorded implementation commits before this resume note:

- `870421b7`: stop tracking 1,104 generated Wi-Fi build files and stale golden output;
  local Wi-Fi build files remain physically present and ignored.
- `0d998cc9`: checked ELF/image/picture parser boundaries.
- `05f6d828`: bounded DMA/peripherals, crypto ownership and shared interrupt arbiter.
- `bd81c4ec`: network bounds, TCP retransmission/write buffering and shared packets.
- `4209290a`: scheduler/host input boundaries and focused machine modules.
- `a521ed2e`: shared CLI/browser contracts and persistent run limits.
- `a9099b4f`: JIT admission/memory helpers and test decomposition.
- `e3319364`: ISA decoding, FFT operands, interrupts and RV32 reserved shifts.
- `29f70fa6`: documentation corrections and scheduler stop regressions.

## Subagent work preserved

All subagents edited these shared files or the separate performance worktree;
their work is not confined to their conversations. Test logs below are relative
to this worktree and remain on disk, mostly under ignored `work/`.

| Lane | Saved implementation and important fact-check | Validation/remaining receipt |
| --- | --- | --- |
| Parsers | ELF validates entire tables and bounded strings; image validates final payload; BMP validates extent before allocation. Empty payload/BSS/name compatibility retained. | `work/parser-check-0919/native-tests.log` 11 pass; `wasm-tests.log` four actual wasm32 cases passed before final empty-table compatibility edit. Five real ROM/app ELFs parsed. |
| Scheduler | Immediate sibling-reset stop with only executed cycles charged; BLOCK fallback during single stepping; first-fetch/sleeping breakpoints; checked camera and bounded knob; modeled/web modules split. | `work/scheduler-tests.log`, `scheduler-cost-model.log` (17), `scheduler-vq-wake.log`, `scheduler-vq-stops.log`, `scheduler-reset-time.log`. Known WAITI blocker above remains. Cooja reset-only time-cap regression passed after partial-time fix. |
| DMA/peripherals | Shared bounded descriptor access/gather/scatter and mapped payload checks; AES math moved into accelerator; S3 T1 routes; chip-specific C3 RTC WDT; RMT/GPIO bounds; correct I2S format/rate. | `work/dma-periph-tests-final.log` 186 pass excluding NAT, `dma-final-postreview.log` 59 bus tests, `rmt-release-tests.log` 11 pass. Audio goldens pending. C3/C6 have one timer/group. RMT no-wrap raises MEM_EMPTY/ERR, not TX_END. TIMG watchdog execution remains documented unsupported. |
| Crypto | Shared SHA1 compression/AES rounds, fixed-size key schedule, SHA block-count and RSA length masks. AES modes owned by device. | `work/crypto-tests.log` 22 pass incl NIST modes and SHA224 vector. SHA224 allegation false: distinct IV + SHA256 compression is correct. RSA was not duplicated; deleting crypto.rs would remove WiFi primitives. |
| Network | Strict shared AP parser, AUTH/EAPOL and IPv4 bounds, one bounded SYN/data/FIN retransmission queue, lossless WouldBlock buffer, 8 connect permits, UDP peer filtering, named WPA stages, canonical packet builder. | `work/network-check-0919/network-lib-tests.log` all 104 S3 library tests pass with socket permission. NAT default preserved as documented product behavior. WiFi CRC32 FCS is not Internet checksum duplication. |
| CLI/Web | Budget survives resets; Cooja honors time/reboot and rejects unsupported max-insns; unique golden temp paths; strict UPDATE_GOLDENS; shared JSON/console/stubs; mutex-safe snapshots, Origin filtering, pong, confined static files and worker failed-network cleanup. | `work/cli-review-tests.log` 6 unit +9 Cooja pass, one external ignored; `work/worker-review-tests.log`; 21 esp-soc library tests passed. Missing Origin remains valid for native tools. S3 app default retained because already logged. |
| WASM ABI | 57 export names preserved; concrete chip enum replaces Any; focused ABI modules; shared LoadKind includes network script/camera; preallocation memory bounds; canonical SRAM emitter classification. | `work/test-receipts/wasm-api-native.log` 12 pass, 3 ROM-dependent ignored. Full final WASM gate still needed. |
| JIT quality | Shared admission/CP/window/loop proofs, scalar/PIE TLB helper, actual layout constants, focused named-case tests. Newly found native wrapping-address guard fixed with subtraction bounds. | `work/test-receipts/wasm-jit-tests-after.log` 78,861 cases; `wasm-jit-tests-no-inline-profile-runtime.log` 78,842. Native 4 regression +6 semantics tests pass. The +28 default cases are upstream #86 architectural stops. Current artifact is alternate no-inline/profile build: rebuild default. Fixed 32-byte TLB assertion would be wrong on wasm32 (28 bytes). |
| ISA | CUST0/1 resolve actual F64 PIE table entries; FFT store concatenation and distinct Upd operand; external edge latching/NMI; reserved RV32 compressed shifts reject. | `work/isa-review/final-tests.log` full Xtensa/RV32 crates pass, optional corpora ignored. Final test-only F64 gate change needs rerun. Blanket pair<4 and blanket CUST illegal were incorrect. Official core config enables MAC16: removal rejected. |
| Cross-architecture | One C3/C6 interrupt Lines arbiter; flash/boot/MMU bounds; host input IRQ invalidation; C3 UART1 output; shared touch transitions; C6 multipan address/PAN must match same slot. | Arbiter 2, chip contracts 5, touch 7 and radio 17 tests pass. Blanket bus trait/core, I2C-device replacement and CO5300 replacement rejected due distinct semantics or added indirection. |
| Synthesis | Draft requested at `docs/reviews/2026-09-19-synthesis.md`; check whether agent saved it before pause. | Must still finish pending items, verification and fresh audience review. |

## Performance state

Read `docs/experiments.md` before any retry. Candidate relates to EX042: signed-byte
dot-product reduction is safely bounded in i32, then widened once; a second simple
refinement uses contiguous 64-bit ACCX load/store with explicit 40-bit masking.
Candidate commit `e1233676` has both. It passed 79,009 differential cases; its full
paired throughput result is still pending.

The S8-only single-pair screen was 83.438 -> 81.208 seconds (2.67% lower), with
10,073,833,775 identical retired instructions and matching console/provenance.
This is only a screen, not a confirmed speedup. Receipts in
`../pocket-perf-runs/s8-screen/summary.json`; original binaries/source diff preserved.

Anomaly lead from performance subagent: older combined4 result published 737 frames,
current baseline published 3,094 (4.20x), despite equal guest work/time and same
Chrome version. PR89 computes a virtual board display rate and u64 division on every
64-instruction round, and raises AMOLED publication cadence. WASM always installs a
queued web sink; battery JS discards frames only after Rust allocates/serializes
them. Preserve smooth product behavior; screen caching the display interval or next
deadline rather than recalculating each round. Other leads: build helper-table copy
only when optional cache timing is active; specialize optional PIE-mode guard only
if configuration immutability/invalidation makes it safe. No causal speedup proven.

The historical 48.5-second claim has no located raw host receipt. Older retained
main binaries had identical standard WASM sections despite different debug metadata.
Do not label a source regression based solely on those historical times.

## Reproduction and environment

- Use `uv` for every Python operation, with a venv (existing root `.venv` or create one).
- System temp paths are denied by a hook: use `TMPDIR="$PWD/work/tmp"` after mkdir.
- Git network access and loopback socket tests need sandbox escalation here.
- Do not time browser pairs during builds or other CPU-heavy simulator jobs. Other
  user agents also run tests; never kill their processes. Our task process scan at
  pause found no matching benchmark/build paths.
- Full suite command: `TMPDIR="$PWD/work/tmp" cargo test --release --workspace -- --include-ignored --skip external_`.
- Rebuild default production WASM with `tools/wasm-build.sh`; default differential
  suite with `tools/wasm-jit-test.sh`. Check `tools/wasm-test.mjs` for manifest runs.
- Need fresh clippy, JS worker/benchmark tests and final review after all changes.
- No new PR yet. If publishing dependent PRs, inspect/use `gh stack` as AGENTS.md
  requires; never rewrite published PR heads just to register a stack.

## Source reviews

The two pasted attachments remain at
`/Users/alice/.codex/attachments/3b0fcdc6-fd16-4697-ad02-40fe77a33c00/Pasted text.txt`
and `/Users/alice/.codex/attachments/a1498cae-98dd-409c-83ca-5e644bbc142c/Pasted text.txt`.
Third review: `/Users/alice/src/a/esp32sim/.pi/plans/2026-09-19-thermonuclear-review/review.md`.
Preserve all numbered bug dispositions plus suggestions/nits and structural T1–T16.
Do not treat a passing old test suite as proof that no correctness defect existed.
