# Review and Pocket Tank work: current checkpoint

Resumed September 19, 2026. Review fixes are committed and validated. Performance
experiments are still running separately. Nothing from this task has been pushed
and no PR has been created.

## Review integration

- Worktree: `/Users/alice/src/a/esp32sim/work/review-perf-0919`.
- Branch: `codex/review-perf-0919`.
- Validated source: `d549d4b27b927e7bd32bd2e4e9cc4188738c8e03`.
- [Full synthesis](docs/reviews/2026-09-19-synthesis.md) covers the three source
  reviews, accepted corrections, rejected remedies and remaining hardware gaps.
- [Validation receipt](docs/evidence/review-2026-09-19/validation.json) records
  commands, source/toolchain/input identities, outcomes and raw-log hashes.
- Release workspace: 371 tests passed, including all 14 golden tests; 12 tests
  requiring external prerequisites were filtered out. Clippy passed. Browser host
  checks: 16 Node tests, 12 Python tests and worker scripts passed. Default WASM:
  eight firmware manifests plus JIT handoff and 78,861 differential cases passed.
- The WAITI partial-virtual-round underflow is fixed with the round-entry idle
  snapshot. A regression failed before the fix; explicit native VQ testing passed
  28 targeted tests, including 64 WAITI scenarios with VQ off/on.
- C3/C6 reboot instruction fixtures now equal 12 seconds * 160 MHz. The old
  scheduler charged unexecuted reset-round cycles. Console/audio/hash fixtures
  were unchanged. Full golden rerun passed.
- Exact F64 PIE CP0/CP3 gating remains unresolved. Current product gating is
  preserved; both coprocessors are enabled in the functional regression. Official
  metadata does not distinguish their requirements. Do not infer silicon behavior.
- Production WASM build preceded three equivalent Clippy rewrites; the differential
  rebuild included them. The retained patch and hashes identify that distinction.

## Open PRs

Fresh checks still found #85, #86, #87, #88, #89, #91, #92 and #93. All recorded
heads are ancestors of the validated source. Their exact hashes are in the receipt.
Merge `de1ccc49` incorporated the newer #86–#89 ancestry without source changes,
because their reviewed fixes had already been integrated alongside our refactors.
Recheck GitHub before any later final integration or publishing. Do not rewrite
published history. Use `gh stack` if publishing dependent PRs.

## Performance state and next action

- Main experiment worktree: `/Users/alice/src/a/esp32sim/work/pocket-perf-0919`.
- Frozen baseline: `/Users/alice/src/a/esp32sim/work/pocket-baseline-0919`,
  `cfa669c4461e6c5b06f9740b2554686830c8d8de`.
- Raw artifacts: `/Users/alice/src/a/esp32sim/work/pocket-perf-runs`.
- Read the [canonical catalog](docs/experiments.md) before proposing or retrying
  anything. Record negative results in the existing entry, not a second list.
- Combined signed-byte reduction plus ACCX access (`e1233676`) passed 79,009
  differential cases but screened flat: 79.4519 -> 79.3696 seconds, identical
  10,073,833,775 instructions, 3,094 binary frames and console. Parked, not adopted.
- Signed-byte-only earlier screen was 2.67% lower wall time. It is preliminary;
  the current host baseline differs, so do not pool absolute times across pauses.
- Isolated display-interval caching (`608b9621`, EX117 refinement) is being timed.
  It preserves cadence and avoids recalculating a board virtual call/u64 division
  every scheduler round. Three focused cadence contracts and existing machine
  tests pass. See the performance agent's resume file for the current result.
- A separate minimal helper-table-copy candidate is under static investigation;
  no builds are permitted during the performance timing window.
- Port only a retained measured change to this integration tree. Run appropriate
  correctness checks after the port. A speed result on the frozen baseline does
  not establish the same percentage on the review integration.

## Coordination and environment

The performance agent owns the CPU-quiet window. Root and other agents may do
light documentation/source work, but no builds or emulator runs until it releases
that window. Do not stop unrelated agents' processes.

Use workspace `work/tmp` for TMPDIR. All Python operations use `uv` and the existing
root `.venv` (or a new explicit venv). Loopback socket tests and GitHub access need
sandbox escalation here. Keep raw captures local until a verified archive exists;
compact evidence files must stay below 50 KB.

All prior subagent work is in committed source or the separate performance
worktrees. The original detailed pause handoff remains available in commit
`228a8d6d`; the current synthesis supersedes its pending-review statuses.
